import React from 'react';

const LoginFailed = () => {
  return (
    <div>
      <h2>Login Failed</h2>
      <p>Sorry, we couldn't log you in. Please check your credentials and try again.</p>
      {/* You can add additional elements or styling for the failed login message */}
    </div>
  );
};

export default LoginFailed;
